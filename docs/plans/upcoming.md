* Add a workflow that allows 
* permit edit of conf.py file
* Transfer preflight and midflight commands to a JSON file for validation
* Provide a referencable JSON schema for bundle.json
* Add a Sphinx Stack set of documentation
* Refine default configuration bundles